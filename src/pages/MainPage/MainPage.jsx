import MainScreenSection from '../../components/MainScreenSection/MainScreenSection';

export default function MainPage({ array1, array2, wage }) {
  return <MainScreenSection array1={array1} array2={array2} wage={wage} />;
}
